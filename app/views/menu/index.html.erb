<h1>Menu</h1>

<table>
  <tr>
    <th>Item</th>
    <th>Price</th>
  </tr>
  <% @menu.each do |key, val| %>
    <tr>
        <td><%= link_to key.capitalize, menu_item_path(key) %></td>
        <td><%= "$%.2f" % val %></td>
    </tr>
  <% end %>
</table>
